generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id       Int @id
  name     String @unique
  password String
  email    String @unique
  active   Boolean

  @@map("users")
}